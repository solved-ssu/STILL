# STILL Design Direction

STILL은 홍보용 랜딩 페이지가 아니라 여러 사람이 오래 쓰는 학습 작업 공간처럼 보여야 한다. 화면의 목적은 인상을 남기는 것이 아니라 자료의 위치와 관계를 빠르게 이해시키는 것이다.

## 1. Voice

- 짧고 구체적인 한국어를 쓴다.
- “혁신”, “함께 성장”, “새로운 경험” 같은 범용 마케팅 문구를 쓰지 않는다.
- 제목은 기능이나 현재 상태를 말한다: `노트 그래프`, `최근 수정`, `내 문서`.
- 영문 대문자 장식용 eyebrow와 불필요한 슬로건을 사용하지 않는다.

## 2. Typography

- 모든 화면과 에디터에 `Pretendard`만 사용한다.
- 본문 14–16px, 보조 정보 12–13px, 페이지 제목 28–36px를 기본으로 한다.
- 굵기는 400·500·600·700 위주로 제한한다. 900 굵기와 과도한 음수 자간을 피한다.
- 코드도 별도 폰트 요청 전까지 Pretendard를 유지한다.

## 3. Color

- 배경: `#f6f7f5`, 표면: `#ffffff`, 구분선: `#dfe2de`.
- 본문: `#202522`, 보조: `#626863`, 강조: `#315c50`.
- 상태 표시를 제외하면 한 화면에 채도가 높은 색을 여러 개 쓰지 않는다.
- 그라데이션, 글로우, 유리 효과를 사용하지 않는다.

## 4. Shape and Depth

- 기본 모서리는 2–6px다. pill은 필터·상태처럼 의미가 있을 때만 쓴다.
- 카드마다 그림자를 넣지 않는다. 계층은 여백과 1px 선으로 구분한다.
- 콘텐츠를 무조건 카드로 감싸지 않는다. 목록은 행과 구분선을 우선한다.
- 장식만을 위한 큰 아이콘·이모지·배경 원을 사용하지 않는다.

## 5. Layout

- 데스크톱은 232px 탐색 영역과 콘텐츠 영역으로 나눈다.
- 콘텐츠 최대 폭은 1180px이며 왼쪽 정렬을 기본으로 한다.
- 홈의 첫 정보는 인사말이 아니라 현재 자료 구조와 최근 변경이다.
- 모바일에서는 탐색 영역을 숨기고 핵심 작업만 상단에 둔다.

## 6. Knowledge Graph

- 중앙 `STILL` 노드 → 분야 노드 → 문서 노드의 관계를 표현한다.
- 분야별 색은 낮은 채도의 고정 팔레트를 사용한다.
- 필터, 확대·축소, 키보드 포커스, 실제 페이지 링크를 제공한다.
- 단순 장식 그래프가 되지 않도록 문서 추가와 동시에 데이터가 반영되어야 한다.

## 7. Avoid the “AI-generated UI” Look

- 거대한 영웅 문구와 세 개짜리 기능 카드 조합
- 모든 요소의 큰 둥근 모서리
- 보라색/청록색 그라데이션과 흐린 빛 효과
- 의미 없는 반짝이 아이콘, 영문 대문자 라벨, 과한 이모지
- 같은 위계의 정보를 여러 겹 카드에 중첩하기
- 실제 동작하지 않는 검색창이나 버튼을 완성된 기능처럼 표시하기

새 화면은 이 문서를 기준으로 검토한다. 예외가 필요하면 시각적 취향이 아니라 사용성 이유를 코드 리뷰에 적는다.
